#include <iostream>
using namespace std;

int main()
{
	char ch;
	cout<<"Enter  character in uppercase: "; 
        cin>>ch;
	
	ch=ch+32;
	cout<<"Entered character in lowercase is: "<<ch;
	return 0;
}
